#!/bin/bash
echo "sudo docker container run -m 600M --rm -it googlenet_latency:v01"
sudo docker container run -m 600M --rm -it googlenet_latency:v01

echo "sudo docker container run -m 550M --rm -it googlenet_latency:v01"
sudo docker container run -m 550M --rm -it googlenet_latency:v01

echo "sudo docker container run -m 500M --rm -it googlenet_latency:v01"
sudo docker container run -m 500M --rm -it googlenet_latency:v01

echo "sudo docker container run -m 450M --rm -it googlenet_latency:v01"
sudo docker container run -m 450M --rm -it googlenet_latency:v01

echo "sudo docker container run -m 400M --rm -it googlenet_latency:v01"
sudo docker container run -m 400M --rm -it googlenet_latency:v01

echo "sudo docker container run -m 350M --rm -it googlenet_latency:v01"
sudo docker container run -m 350M --rm -it googlenet_latency:v01

echo "sudo docker container run -m 300M --rm -it googlenet_latency:v01"
sudo docker container run -m 300M --rm -it googlenet_latency:v01

echo "sudo docker container run -m 250M --rm -it googlenet_latency:v01"
sudo docker container run -m 250M --rm -it googlenet_latency:v01

echo "sudo docker container run -m 200M --rm -it googlenet_latency:v01"
sudo docker container run -m 200M --rm -it googlenet_latency:v01
